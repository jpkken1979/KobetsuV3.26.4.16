---
name: DataTotal carpeta de exports — siempre commitear
description: DataTotal/ contiene exports importantes y se versiona (repo privado)
type: feedback
---

DataTotal/ es una carpeta de exports importantes. NUNCA ignorarla ni excluirla de commits. El repo es privado, por lo que se puede subir sin problema.

**Why:** El usuario lo pidió explícitamente — los archivos de DataTotal son datos de referencia valiosos.

**How to apply:** En cualquier `git status` o `git add`, incluir siempre `DataTotal/` junto con el resto de los cambios.
