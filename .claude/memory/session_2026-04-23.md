---
name: Sesion 2026-04-23 — Refactor completo KobetsuV3
description: Split 7 archivos grandes + cleanup dead code + investigacion legacy
type: project
auto_saved: true
trigger: session
date: 2026-04-23
---

## Que se hizo
- R-1: Eliminado wrapper `documents-generate-batch.ts` (re-export puro)
- R-2: `validateForPdf` en `validation.ts` marcado como test-only
- R-3: `cleanupOrphanedFiles` no existe — la función activa es `cleanupPurgedContractDocuments`
- R-4: `batch-contracts.ts` (969L) → `batch-contracts/write.ts` + `read.ts` + `cancel.ts` + `types.ts`
- R-5: `factories.ts` (864L) → `factories/crud.ts` + `roles.ts` + `calendars.ts` + `cascade.ts` + `excel.ts`
- R-6: `koritsu-pdf-parser.ts` dividido en Excel + PDF + koritsu-types
- R-7: `import-factories-service.ts` (679L) → `import-factories/parser.ts` + `validator.ts` + `writer.ts`
- P-9: `shouheisha.tsx` spliteado (1194L→615L) en 8 componentes hijos con barrel export
- Fix koritsu encoding: katakana の (U+7684) → hiragana 務 (U+52D9) en regex

## Decisiones tecnicas
- Archivos originales convertidos en pure re-export facades para backward compatibility
- `shouheisha/index.ts` renombrado a `-index.ts` (con guión) porque TanStack Router lo trata como ruta espuria
- Rate priority chain: `billingRate ?? hourlyRate ?? factory.hourlyRate` — P-6/P-7 no se automatizan, usuario maneja manualmente via shaindaicho
- `employee-mapper.ts` (PDF) y `import-employees.ts` (Excel→DB) son complementarios, NO se unifican

## Descubrimientos
- L-1: `/takao-reentries` endpoint NO existe — lógica vive en `takao-detection.ts` usada en POST /import
- L-2: Koritsu import es ACTIVO — frontend `companies/koritsu.tsx` lo usa plenamente
- L-3: 13 `contract_employees.hourly_rate` NULL (todos KOB-202604-0004兄妹, billing_rate=1750)
- L-3: 4 empleados sin rates (DAO VIET CHIEN, CAO ANH DI, LUONG XUAN TUAN, DAO CONG PHUC) — huérfanos sin factory_id

## Pendiente
- P-6/P-7: Manejo manual de rates via shaindaicho (no se automatiza)
- P-8: 6 contratos cancelled sin campos Art.26 — cuando el usuario quiera remediar, avisa

## Commits
- `fec6937` refactor(services): split koritsu-pdf-parser into types/pdf/excel modules
- `bd01125` refactor(services): split import-factories-service into parser/validator/writer
- `12174c6` refactor(routes): split factories.ts into crud/roles/calendars/cascade/excel submodules
- `ca8a803` refactor(frontend): split shouheisha.tsx en componentes hijos
- `6f65c64` refactor: split large files + cleanup dead code
- `a130167` fix(koritsu): encoding 工的→務 en regex de parsing
