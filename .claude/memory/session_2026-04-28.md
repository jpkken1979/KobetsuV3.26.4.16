---
name: Sesion 2026-04-28 — Expansión de CLAUDE.md con 8 mejoras
description: Completar mejoras a CLAUDE.md, documentación expandida con Quick Start, Testing, TypeScript, SDD, Drift Guard
type: project
auto_saved: true
trigger: session
date: 2026-04-28
---

## Qué se hizo

- Expandido "Quick Start & Key Commands" con explicaciones detalladas de cada comando npm
  - Notas de seguridad: `--env-file .env` es REQUERIDO para `npm run dev` (sin él, `ADMIN_TOKEN` y env vars no cargan)
  - Separación clara entre `dev` (concurrently), `dev:server` y `dev:client` (para Windows fallback)
  - Aclaración de `test:prepare` es DESTRUCTIVO pero aislado de la DB de producción

- Mejora sustancial de "Testing" section
  - Explicación de Vitest framework, serial execution (`--no-file-parallelism`)
  - Test DB isolation: `DATABASE_PATH=data/kobetsu.test.db` via `cross-env`
  - Coverage thresholds per-file: contract-dates 95%, batch-helpers 85%, koritsu-pdf-parser 80%
  - PDF smoke tests, mocking philosophy (prefer real SQLite), CI pipeline

- Nueva sección "TypeScript Standards"
  - Resuelve confusión path-scoping (rules en `.claude/rules/typescript.md` aplican SOLO a `nexus-app/`)
  - Clarifica strict mode, no `any`, functional components, motion variants, Tailwind v4

- Expansión "Adding New Routes"
  - TanStack Router file-based routing: explicación de mapeo archivo→route
  - Advertencia: `src/routeTree.gen.ts` es **auto-generado** — nunca editar manualmente

- Nueva sección "Drift Guard Test"
  - Documenta test en `server/__tests__/claude-md-drift.test.ts`
  - Explica que valida route files (N) y service modules (N) en CLAUDE.md vs filesystem
  - Cuando falla, fuerza actualización de documentación

- Expansión "Auto-Injected Rules"
  - Explica que `.claude/rules/*.md` se inyectan automáticamente en cada sesión
  - Las reglas siempre ganan sobre comportamiento genérico

- Mejora "Adding New Features (Complex Tasks)"
  - Guidance cuando usar SDD workflow (3+ files, architectural decisions)
  - Cuando NO usar (bug fixes, simple additions, docs-only)

- Commit exitoso: `d742fc5` con todos los cambios

## Decisiones técnicas

- **Mantener estructura existente**: no reorganizar las secciones, solo expandir las relevantes
- **Referencias a reglas externas**: en vez de duplicar, referenciar `.claude/rules/typescript.md` y explicar scope
- **Safety notes prominentes**: `--env-file .env` es critico para `npm run dev`, documentar conspicuamente
- **Test DB isolation**: aclarar que `test:prepare` es seguro correr mientras dev server está arriba
- **Auto-injected rules**: explicar mecanismo para que futuras sesiones entiendan por qué reglas override defaults

## Descubrimientos

- `npm run dev` sin `--env-file .env` causa que ADMIN_TOKEN no cargue → adminGuardMiddleware rechaza localhost
  - Esto ya fue un problema resuelto en sesión 2026-04-15b, documentado en memory
  - CLAUDE.md ahora lo captura prominentemente en Quick Start

- TypeScript rules path-scoping (`.claude/rules/typescript.md`) solo aplica a `nexus-app/`, no a `src/`
  - Confusión histórica, ahora aclarada en nueva sección "TypeScript Standards"

## Pendiente

- Nada — sesión completa. CLAUDE.md está actualizado y commiteado.

## Commits incluidos

- `d742fc5` — docs(CLAUDE): expandir secciones Quick Start, Testing, TypeScript, SDD y drift guard
