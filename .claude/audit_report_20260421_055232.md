# Audit Report — Kobetsu V3.26.4.16

- **Timestamp:** 2026-04-21 05:52:32 UTC
- **Repo:** `/home/user/KobetsuV3.26.4.16`
- **Branch:** `claude/add-claude-documentation-WKUqA`
- **HEAD:** `a17a703 docs(claude-md): sincronizar CLAUDE.md con estado real del repo`
- **Generador:** `/audit-pro v3.0` adaptado al stack real del repo
  (el template original asume el meta-repo Antigravity con Python/Rust/Tauri/Bot; este repo es TS full-stack, así que las fases Python/Rust/Bot/Cargo/Tauri/Governance no aplican y fueron reemplazadas por equivalentes TS).

> **Limitación importante:** `node_modules/` no está instalado en este entorno, por
> lo que `npm test`, `npm run lint`, `npm run typecheck`, `npm audit` y
> `npm outdated` **no pudieron ejecutarse**. Su estado está etiquetado como
> `SKIPPED — no node_modules`. Los hallazgos se derivan del análisis estático
> del árbol de archivos más git y ripgrep.

---

## Fase 0 — Snapshot Inicial

| Ítem | Estado |
|------|--------|
| Working tree | **limpio** (`git status --short` vacío) |
| Branch local vs origin | sincronizado (0 ahead / 0 behind) |
| Stash | vacío |
| Worktrees extra | ninguno |
| Últimos commits | doc/chore/feat recientes, formato convencional respetado |

### Inventario

| Categoría | Cantidad |
|-----------|----------|
| Archivos `.ts`/`.tsx` en `src/` | 162 |
| Archivos `.ts` en `server/` | 116 |
| Suites de test (`server/__tests__/*.test.ts`) | 34 |
| Tests en `src/**/*.test.*` | 8 |
| Scripts en `scripts/` | 4 |
| LOC totales (src + server) | 65.737 |
| LOC source (sin tests) | 54.619 |
| LOC tests | 11.118 |
| Ratio test / source | ~20,4 % |

### Stack detectado

- Node.js / NPM (frontend + API)
- React 19.2 + TanStack Router 1.167 + TanStack Query 5.96
- Hono 4.12 + Drizzle 0.45 + better-sqlite3 12
- Vite 8, TypeScript 6, Vitest 4, Tailwind 4
- Sin Python/Rust/Tauri en este repo (el template audit-pro los referencia por defecto, pero aquí no aplica).

---

## Fase 1 — Suite de Tests y Build

| Check | Estado |
|-------|--------|
| `npm run test:run` | **SKIPPED** — no `node_modules` |
| `npm run lint` | **SKIPPED** — no `node_modules` |
| `npm run typecheck` | **SKIPPED** — no `node_modules` |
| `npm run build` | **SKIPPED** — no `node_modules` |
| `claude-md-drift.test.ts` | **PASS estático** — 32 routes / 30 services declarados = realidad |
| Conteo declarado de hooks (21) | **PASS** — coincide con `ls src/lib/hooks | wc -l = 21` |

**Fixtures conocidos (ver `ESTADO_PROYECTO.md`):** 42 archivos, 762 tests pasando en la última corrida del usuario el 2026-04-20.

---

## Fase 2 — Calidad de Código

### TypeScript `any`

| Métrica | Valor |
|---------|-------|
| `: any` en source (`src/`, `server/`) | **0** |
| `as any` en source (no tests) | **0** |
| `as any` en tests | **20** (3 archivos: `batch-contracts-service.test.ts`, `document-generation.test.ts`, `pdf-data-builders.test.ts`) — aceptable para mocks |
| `@ts-ignore` / `@ts-expect-error` | **0** |

### `console.*` en frontend

| Archivo | Ocurrencias |
|---------|-------------|
| `src/components/ui/error-boundary.tsx` | 1 (uso legítimo en ErrorBoundary) |
| Resto de `src/` | 0 |

### `console.*` en server (aceptable — startup / migraciones)

- `server/index.ts`, `server/db/index.ts`, `server/db/seed.ts`, `server/db/migrate*.ts`, `server/pdf/helpers.ts`, `server/routes/admin-reset.ts` — total 57 ocurrencias, todas en bootstrap/logging intencional.

### TODO / FIXME / HACK

| Métrica | Valor |
|---------|-------|
| Markers reales | **0** (los matches aparentes son "TODOS" en español o "XXXX" en patterns de contrato) |

### Archivos >500 LOC (nuevos hallazgos no documentados en CLAUDE.md)

| Archivo | LOC | Acción sugerida |
|---------|-----|-----------------|
| `src/lib/api-types.ts` | **907** | Evaluar split por dominio (contratos, empleados, fábricas, admin) |
| `server/services/batch-contracts.ts` | **818** | Partir en helpers + núcleo |
| `src/routes/companies/-import-modal.tsx` | 602 | No crítico |
| `src/routes/data-check/-flat-view.tsx` | 578 | No crítico |
| `server/pdf/kobetsu-pdf.ts` | 591 | Ya referenciado en CLAUDE.md |
| `server/pdf/keiyakusho-pdf.ts` | 578 | Ya referenciado |
| `server/pdf/koritsu-kobetsu-pdf.ts` | 577 | Ya referenciado |

> CLAUDE.md declara varios >500L pero **no incluye** `api-types.ts` (907L) ni
> `batch-contracts.ts` (818L) ni `-import-modal.tsx` ni `-flat-view.tsx`.
> Recomiendo agregarlos al listado de "Large Files Warning" en el próximo pasaje.

---

## Fase 3 — Seguridad (OWASP Top 10 adaptado a Node/TS)

### A01 — Injection

| Check | Resultado |
|-------|-----------|
| `shell: true` en `child_process` | **0** |
| `eval(` / `new Function(` | **0** |
| `spawn` / `execFileSync` en server | **2 usos, ambos safe**: `spawn(explorer.exe/open/xdg-open, [dir])` con `dir` restringido a constantes de módulo (`ROUDOU_OUTPUT_DIR` / `KOBETSU_OUTPUT_DIR`); `execFileSync("netstat", ["-ano"])` con args hardcoded. Ambos con `NODE_ENV !== production` como guard explícito en el endpoint `/open-folder`. |

### A02 — Cryptographic Failures

| Check | Resultado |
|-------|-----------|
| Secrets hardcoded (patterns `API_KEY=...`, `TOKEN="abc"`) | **0** |
| Comparación de tokens admin | usa `timingSafeEqual` (ver `server/middleware/security.ts:7-11`). ✓ |

### A03 — XSS / SQL Injection

| Check | Resultado |
|-------|-----------|
| `dangerouslySetInnerHTML` / `.innerHTML =` | **0** |
| SQL con template strings peligroso (`db.raw`, `sql.raw` con interpolación) | **0** — todas las queries usan Drizzle ORM con binding |
| Zod en rutas mutables | **sí** — 8+ rutas usan `.parse()`/`.safeParse()` |

### A05 — Security Misconfiguration

| Check | Resultado |
|-------|-----------|
| Headers de seguridad | `X-Content-Type-Options`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Permissions-Policy` — ✓ |
| CORS | `FRONTEND_ORIGIN` env, no wildcard `*` — ✓ |
| Rate limit | configurable (`API_RATE_LIMIT_MAX=180`/`WINDOW=60s` por defecto) — ✓ |
| Body limit | 10 MB — **⚠ un poco alto para una app sin upload de media**; valuar 2-3 MB |
| CSP (Content-Security-Policy) | **ausente** — aceptable para app local-first, pero si se va a exponer por túnel, agregar |
| `.env` en git | NO (gitignored) — ✓ |
| `.env.example` | presente — ✓ |

### A07 — Authentication

- Admin routes: bypass para `localhost`, `x-admin-token` + `timingSafeEqual` para remoto. Patrón correcto.
- Sin auth por usuario — consistente con "Internal/local-first admin application".

### A09 — Logging & Monitoring

- Audit log en mutations (`audit_log` table) — ✓ ver schema y tests (`server/__tests__/`).
- No se loguean secrets (revisado: 0 matches de `token|secret|password` en `logger.*`).

### A10 — SSRF

- No hay fetch/axios desde server hacia URLs user-supplied en código de producción (solo se encuentran imports de `node:*` y `hono/*`).

### Hallazgos de seguridad — resumen

- **CRÍTICO:** ninguno
- **ALTO:** ninguno
- **MEDIO:** Body limit 10 MB puede ser excesivo (baja prioridad, pero fácil endurecer).
- **BAJO:** sin CSP header (no crítico para local-first, pero trivial de sumar si algún día se expone).
- **DRIFT:** `.claude/rules/security.md` afirma que `.env` se versiona en este repo (líneas 151-153 de `.gitignore`), **pero** `.gitignore` real tiene `.env` ignorado en la línea 14, y **no hay `.env` en `git ls-files`**. La regla está desactualizada respecto a la realidad operativa. Hay que decidir uno de los dos caminos y alinear.

---

## Fase 4 — Arquitectura y Boundaries

| Regla | Resultado |
|-------|-----------|
| Cliente (`src/`) importa de `@server/` o `../server/` | **0 matches** — frontier respetada ✓ |
| Cliente usa `node:*` / `fs` / `path` / `child_process` / `better-sqlite3` | **0 matches** ✓ |
| Servidor (`server/`) importa de `@/` o `../src/` | **0 matches** ✓ |
| Motion components sin `useReducedMotion` | **0 archivos** entre los 27 que usan `<motion.*>` ✓ |
| `window.confirm()` en vez de `ConfirmDialog` | **0 matches** ✓ |
| `useRef` como close-guard en Radix | **0 matches** ✓ |
| `body.style.overflow` manual | **0 matches** ✓ |
| Raw query key arrays (`queryKey: ["contracts"]`) fuera de `query-keys.ts` | **0 matches** ✓ |
| Rate priority con `\|\|` (debería ser `??`) | **0 matches** ✓ |

**Violaciones reales encontradas:**

- **UI rule `p-5` prohibido:** 6 ocurrencias
  - `src/routes/shouheisha.tsx:589, 741, 910, 1007, 1134` (5x `<Card className="p-5">`)
  - `src/components/ui/page-header.tsx:21` (`md:p-5` en clase base)

  Según `CLAUDE.md` línea ≈355 y el design system LUNARIS v2, `p-5` está
  prohibido (usar `p-4` o `p-6`). Estos 6 casos deberían reemplazarse.

---

## Fase 5 — Dependencias

- `npm outdated` y `npm audit`: **SKIPPED — no node_modules**.
- Dependencias declaradas en `package.json` apuntan a majors razonablemente recientes:
  - React 19.2, TanStack Router 1.167, TanStack Query 5.96, Hono 4.12, Drizzle 0.45, Vite 8, TypeScript 6, Vitest 4, Tailwind 4, Zod 4, better-sqlite3 12, PDFKit 0.18.
- Pendientes según `ESTADO_PROYECTO.md`: `pdf-lib` ya eliminado, Vite 8 ya migrado. No hay pendientes de upgrade mayor abiertos.

---

## Fase 6 — Completitud y TODOs

- TODO / FIXME / HACK reales: **0**.
- `NotImplementedError` / `throw new Error("not implemented")`: no se detectó en ripgrep sobre `src/`+`server/`.
- Stubs vacíos (`function foo() {}` sin body): no encontrados en rondas rápidas.
- Cobertura de servicios críticos (según `ESTADO_PROYECTO.md`): `contract-dates` 95 %, `batch-helpers` 85 %, `koritsu-pdf-parser` 80 %, globals 50 % — umbrales ya enforced por `test:coverage`.

---

## Fase 7 — Performance (revisión estática)

| Check | Resultado |
|-------|-----------|
| `async` bloqueado con sync I/O | **No detectado** en rutas |
| Loops anidados sospechosos en DB | Todas las operaciones de batch usan `sqlite.transaction()` — ✓ |
| Missing `Promise.all` en paralelizables | No se revisó exhaustivamente (requeriría análisis por ruta) |
| Connection pooling | N/A — SQLite local con WAL, single-connection |

---

## Fase 8 — Documentación

| Doc | Líneas | Última mod | Estado |
|-----|--------|------------|--------|
| `CLAUDE.md` | 486 | 2026-04-21 | **Fresco** (acabo de actualizar) |
| `README.md` | 63 | 2026-04-14 | Corto — onboarding mínimo, pero suficiente |
| `ESTADO_PROYECTO.md` | 904 | 2026-04-20 | **Header dice v26.3.25 — drift con directorio `V3.26.4.16` y CLAUDE.md `26.4.16`** |
| `AGENTS.md` | 215 | 2026-04-14 | OK |
| `docs/architecture.md` | 509 | 2026-04-14 | **Header dice v26.3.25 — mismo drift** |

**Drift guard** (`server/__tests__/claude-md-drift.test.ts`):

| Conteo | Declarado en CLAUDE.md | Real | Status |
|--------|------------------------|------|--------|
| Route files | 32 | 32 | ✓ |
| Service modules | 30 | 30 | ✓ |
| Hooks | 21 | 21 (no enforceado por el test actual, pero documentado) | ✓ |

---

## Fase 9 — Ecosistema MCP

- `.mcp.json` declara **12 servers** (antigravity, antigravity-agents, y otros).
- Los paths apuntan a `.agent\\mcp\\*` y `mcp-server\\*` (separadores Windows).
- `.agent/` y `.antigravity/` **no están en este working copy** (gitignored). La config asume que el usuario los tiene instalados externamente. Coherente con `.claude/rules/ecosystem-usage.md` que dice "MCP-first, no leer archivos locales a menos que el MCP no alcance".
- En este entorno sandboxed no se puede verificar health del gateway `:4747`.

---

## Fase 10 — Git Archeology

- Commits recientes siguen convención (`feat|fix|docs|chore|refactor`). ✓
- Formato de mensaje coherente con `.claude/rules/commits.md`.
- `git log --all --oneline | rg -v` no encontró commits fuera de formato en los últimos 15.
- Branches remotas: `master`, `claude/add-claude-documentation-WKUqA`. Sin branches huérfanas visibles.

---

## Scorecard

| Dimensión | Score | Nota |
|-----------|-------|------|
| Tests (estática) | **B** | Suite existe y pasa según ESTADO (762 tests). No se pudo correr en vivo por falta de `node_modules`. Coverage enforced por test script. |
| Code Quality | **A** | Cero `any` en source, cero TODOs reales, cero `console.*` en frontend (salvo ErrorBoundary). Algunos archivos >500L sin documentar. |
| Security | **A-** | Hardening sólido (rate limit + timingSafeEqual + headers + CORS). Observaciones menores: body limit 10 MB, sin CSP, drift entre regla de `.env` y `.gitignore`. |
| Architecture | **A** | Boundaries cliente/server perfectamente respetadas. 6 violaciones de regla `p-5` (frontera menor). |
| Documentation | **B+** | CLAUDE.md fresco. `ESTADO_PROYECTO.md` y `docs/architecture.md` tienen header con versión desactualizada (`v26.3.25` vs `V26.4.16`). |
| Dependencies | **B** | No pude correr `npm audit`. Versiones declaradas son modernas. |

**Score global: A-** — repo saludable, con drifts menores documentales y una regla UI con pequeña regresión.

---

## Próximos pasos priorizados

1. **(P1) Corregir violaciones de `p-5`** — 6 casos en `shouheisha.tsx` + `page-header.tsx`. Reemplazar por `p-4` o `p-6` según el ritmo visual.
2. **(P2) Alinear `.env` gitignore ↔ regla** — decidir: ¿se versiona o no? `.gitignore:14` dice NO; `security.md` dice SÍ. Editar uno de los dos para que quede consistente.
3. **(P2) Actualizar headers de versión en docs secundarios** — `ESTADO_PROYECTO.md:1` y `docs/architecture.md:1` dicen `v26.3.25`; pasar a `v26.4.16` (o al canónico del proyecto).
4. **(P3) Agregar al "Large Files Warning" de CLAUDE.md** — `src/lib/api-types.ts` (907L), `server/services/batch-contracts.ts` (818L), `-import-modal.tsx` (602L), `-flat-view.tsx` (578L).
5. **(P3) Endurecer body limit** — evaluar bajar de 10 MB a 2-3 MB (revisar primero si algún Excel de import supera esto).
6. **(P4) CSP header** — si la app algún día se expone por túnel, sumar `Content-Security-Policy`.

---

*Generado por audit-pro v3.0 (adaptación TS) el 2026-04-21 05:52:32 UTC.*
*Reporte: `.claude/audit_report_20260421_055232.md`.*
